import React from "react";
import ReactDOM  from "react-dom";
import AppPricing from "../app/AppPricing";

ReactDOM.render(
    <React.StrictMode>
        <AppPricing />
    </React.StrictMode>, document.querySelector(".root")

)